import logo from "../../assets/logo.svg";

const Navbar = () => {
  return (
    <nav>
      <img src={logo} alt="Logo image" />

      <a href="#">Test</a>

      <a href="#">Test</a>

      <a href="#">Test</a>
    </nav>
  );
};

export default Navbar;
